{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vectorcompiler.dev/schemas/bench_spec_v1.schema.json",
  "title": "Bench spec v1 (synthesize)",
  "description": "Minimal case list for synthesizing or attaching I/O expectations. Full bench manifests also include schema_version, program_path, export, and fuel (see benchmarks/manifests/).",
  "type": "object",
  "additionalProperties": false,
  "required": ["cases"],
  "properties": {
    "cases": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/bench_case" }
    }
  },
  "$defs": {
    "bench_case": {
      "type": "object",
      "additionalProperties": false,
      "required": ["args", "expect_i32"],
      "properties": {
        "args": {
          "type": "array",
          "items": { "type": "integer" },
          "description": "i32 arguments passed to the exported function."
        },
        "expect_i32": {
          "type": "integer",
          "description": "Expected i32 return value for this case."
        }
      }
    }
  }
}
